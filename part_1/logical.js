// && -> and 
// || -> or
// ! -> reverse [just like our not gate.]

/*
lets say we are building an application we need
 to make it logged in

*/

let isLoggedIn = true;
let isPaid = false;
// console.log(isLoggedIn && isPaid);

let isEmailUser = true;
let isGoogleUser = false;

console.log(isEmailUser || isGoogleUser);
